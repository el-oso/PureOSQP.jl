import{_ as t,o as a,c as s,ao as i}from"./chunks/framework.CWHsw1fv.js";const u=JSON.parse('{"title":"Guarantees","description":"","frontmatter":{},"headers":[],"relativePath":"guarantees.md","filePath":"guarantees.md","lastUpdated":null}'),o={name:"guarantees.md"};function n(r,e,c,d,l,h){return a(),s("div",null,[...e[0]||(e[0]=[i('<h1 id="Guarantees" tabindex="-1">Guarantees <a class="header-anchor" href="#Guarantees" aria-label="Permalink to &quot;Guarantees {#Guarantees}&quot;">​</a></h1><p>Three properties of this package are machine-checked rather than asserted: the linear-system interface is a declared contract, the hot path is proven allocation-free and type-stable, and the public entry points compile under <code>juliac --trim</code>.</p><h2 id="The-LinearSystem-contract" tabindex="-1">The <code>LinearSystem</code> contract <a class="header-anchor" href="#The-LinearSystem-contract" aria-label="Permalink to &quot;The `LinearSystem` contract {#The-LinearSystem-contract}&quot;">​</a></h2><p>The factorization backend is an interface, declared with <a href="https://github.com/el-oso/TypeContracts.jl" target="_blank" rel="noreferrer">TypeContracts.jl</a> and enforced at precompilation:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@contract</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LinearSystem </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">begin</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    factorize!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Any</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Bool</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    solve_system!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Any</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Any</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Any</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Nothing</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p><code>@verify ReducedCholesky trim_compat=true</code> and the same for <a href="/PureOSQP.jl/dev/api#PureOSQP.FullKKT"><code>FullKKT</code></a> run when the module precompiles, so a backend that is missing a method — or whose method infers to the wrong return type — fails to load rather than failing at solve time.</p><p>The contract is covered by a test that gives it something to reject: a type that declares the supertype and implements none of it must be reported as unsatisfied. Without that, a contract can be satisfied vacuously and nobody notices.</p><p>To add a backend, subtype <code>LinearSystem</code>, implement the two methods, and <code>@verify</code> it. Note the backend is fixed when the workspace is built, so it is part of the workspace&#39;s type and every per-iteration call dispatches statically — there is no runtime branch on which backend is in use.</p><h2 id="No-allocation,-no-type-instability" tabindex="-1">No allocation, no type instability <a class="header-anchor" href="#No-allocation,-no-type-instability" aria-label="Permalink to &quot;No allocation, no type instability {#No-allocation,-no-type-instability}&quot;">​</a></h2><p><code>bench/strictmode_audit.jl</code> uses <a href="https://github.com/el-oso/StrictMode.jl" target="_blank" rel="noreferrer">StrictMode.jl</a> to check, for <strong>both</strong> backends:</p><table tabindex="0"><thead><tr><th style="text-align:right;">function</th><th style="text-align:right;">guarantees</th></tr></thead><tbody><tr><td style="text-align:right;"><code>admm_step!</code></td><td style="text-align:right;">type-stable, allocation-free</td></tr><tr><td style="text-align:right;"><code>update_residuals!</code></td><td style="text-align:right;">type-stable, allocation-free</td></tr><tr><td style="text-align:right;"><code>solve_system!</code></td><td style="text-align:right;">type-stable, allocation-free</td></tr><tr><td style="text-align:right;"><code>check_termination</code></td><td style="text-align:right;">type-stable</td></tr><tr><td style="text-align:right;"><code>factorize!</code></td><td style="text-align:right;">type-stable</td></tr><tr><td style="text-align:right;"><code>solve!</code></td><td style="text-align:right;">type-stable</td></tr></tbody></table><p>Run it with <code>cd bench &amp;&amp; julia --project=. strictmode_audit.jl</code>.</p><p>Two things about that audit are worth knowing, because both were learned the hard way:</p><p><strong>It runs in <code>:full</code> mode, not <code>:fast</code>.</strong> The fast heuristic reports <code>admm_step!</code> and <code>update_residuals!</code> as allocating when AllocCheck proves they do not. For this package the cheap tier produces false positives, so it cannot be the gate.</p><p><strong>It asserts <code>checks_enabled()</code> before doing anything.</strong> With StrictMode&#39;s checks disabled every assertion expands to the bare call and the audit passes vacuously, printing exactly like a clean run. <code>bench/LocalPreferences.toml</code> enables them, and the script refuses to report a pass without confirming it.</p><p>Getting to a provable <code>noalloc</code> required rewriting the hot path&#39;s broadcasts as explicit loops. Any <code>a .= b .+ c</code> leaves an <code>unaliascopy</code> branch — a copy that would only happen if the destination aliased a source — which never fires at runtime but which AllocCheck must count. Measured with <code>@allocated</code> the broadcasts were already 0 bytes; the loops are what make that a <em>proof</em> rather than an observation.</p><h2 id="trim-compatibility" tabindex="-1"><code>--trim</code> compatibility <a class="header-anchor" href="#trim-compatibility" aria-label="Permalink to &quot;`--trim` compatibility {#trim-compatibility}&quot;">​</a></h2><p><code>juliac --trim</code> needs every call resolved statically. All public entry points are checked with <a href="https://github.com/JuliaLang/TrimCheck.jl" target="_blank" rel="noreferrer">TrimCheck.jl</a>, in <code>test/trim_tests.jl</code>, so a dynamic dispatch or a reflective call cannot creep in unnoticed:</p><ul><li><p><code>solve</code> with default settings, with <code>polish = true</code>, with <code>linsys = :kkt</code>, and with <code>scaling = 0</code>;</p></li><li><p><code>setup</code> → <code>solve!</code> → <code>update!</code> → <code>solve!</code>;</p></li><li><p><code>setup</code> → <code>solve!</code> → <code>warm_start!</code> → <code>solve!</code>.</p></li></ul><p>The same test includes a <strong>negative control</strong> — an entry point that calls <code>Base.return_types</code>, which the trimmer cannot resolve. It must be reported as <em>not</em> compatible. A checker that passed everything would tell you nothing, and this is what proves it is discriminating.</p><p>TypeContracts&#39; <code>@verify</code> at module top level does not interfere: it runs at precompilation and is unreachable from any entry point, so the trimmer removes it.</p><p>Requires Julia ≥ 1.12, which is also this package&#39;s <code>[compat]</code> floor — <code>--trim</code> does not exist before it.</p><h2 id="What-the-guarantees-cover-on-GPU-arrays" tabindex="-1">What the guarantees cover on GPU arrays <a class="header-anchor" href="#What-the-guarantees-cover-on-GPU-arrays" aria-label="Permalink to &quot;What the guarantees cover on GPU arrays {#What-the-guarantees-cover-on-GPU-arrays}&quot;">​</a></h2><p>GPU support is <strong>matrix-free only</strong>. <code>linsys = :indirect</code> is the one backend whose inner solve has a GPU counterpart — Krylov.jl&#39;s <code>cg!</code> is GPU-native, <code>bunchkaufman!</code> has no GPU implementation, CHOLMOD is CPU by construction, and <code>potri!</code> reaches LAPACK. Any other backend is refused at <a href="/PureOSQP.jl/dev/api#PureOSQP.setup"><code>setup</code></a> with a message naming the remedy, rather than left to fail inside <code>factorize!</code>. <code>polish</code> and the derivatives stay on the host for the same reason, and say so: both refuse a GPU workspace by name rather than surfacing a scalar-indexing error from inside <code>bunchkaufman!</code>.</p><p>Two of the three guarantees above carry over unchanged, and one does not:</p><ul><li><p><strong>Type stability</strong> holds and is checked.</p></li><li><p><strong><code>--trim</code></strong> is unaffected. Its entry points are concrete <code>Matrix{Float64}</code>/ <code>Vector{Float64}</code> calls, so GPU code — reachable only through extension methods on GPU types — never enters the binary, the same scoping that keeps CHOLMOD&#39;s <code>ccall</code>s out.</p></li><li><p><strong>Allocation-free does not extend to GPU workspaces</strong>, and is not claimed for them. Every kernel launch allocates, and the broadcast and <code>mapreduce</code> schedules the GPU path uses allocate host-side besides. The claim is about <code>Vector</code>-backed workspaces, which is what <code>bench/strictmode_audit.jl</code> checks.</p></li></ul><p>The elementwise work is written once as scalar functions with two schedules — an indexed loop for <code>Vector</code>, a broadcast or reduction for everything else (<code>src/elementwise.jl</code>). The loops are not a style preference: AllocCheck reports a broadcast as allocating even when its destination is distinct from its sources, because <code>Base.Broadcast</code> keeps an <code>unaliascopy</code> branch it cannot rule out, and it reports a multi-argument <code>mapreduce</code> the same way. Keeping both schedules is what lets the CPU path stay provably allocation-free while the GPU path avoids indexing.</p><p><strong>JLArrays is the correctness gate, and it is not a claim about CUDA.</strong> <code>test/gpu_tests.jl</code> runs the solver under <code>JLArrays.allowscalar(false)</code>, which enforces exactly the discipline CUDA.jl enforces, so a green run certifies that nothing on the solve path indexes an array elementwise. It certifies nothing else: <code>JLArray &lt;: StridedArray</code> is true and <code>cholesky!</code> on one <em>succeeds</em> through CPU LAPACK, so a JLArrays test of a direct backend would pass while the same dispatch died inside <code>unsafe_convert</code> on a CuArray. CuArray is designed for and untested; the refusal above is what keeps that gap from being discovered as a wrong answer.</p>',28)])])}const y=t(o,[["render",n]]);export{u as __pageData,y as default};
