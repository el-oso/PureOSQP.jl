import{_ as s,o as i,c as t,ao as a}from"./chunks/framework.CWHsw1fv.js";const k=JSON.parse('{"title":"Structured operators","description":"","frontmatter":{},"headers":[],"relativePath":"operators.md","filePath":"operators.md","lastUpdated":null}'),n={name:"operators.md"};function r(h,e,d,o,l,c){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="Structured-operators" tabindex="-1">Structured operators <a class="header-anchor" href="#Structured-operators" aria-label="Permalink to &quot;Structured operators {#Structured-operators}&quot;">​</a></h1><p>The solver holds <code>P</code> and <code>A</code> by reference and never copies them, so a matrix that knows its own structure keeps that structure all the way to the point where a backend is chosen. This page is how to bring one: a representation that stores less than <code>m×n</code>, computes its products from what it stores, and is solved by a backend that never forms an <code>n×n</code> object.</p><p>Three examples ship, and each is worth reading as a template for a different shape:</p><table tabindex="0"><thead><tr><th style="text-align:right;">representation</th><th style="text-align:right;">backend</th><th style="text-align:right;">what the structure buys</th></tr></thead><tbody><tr><td style="text-align:right;"><a href="/PureOSQP.jl/dev/api#PureOSQP.BlockDiagonal"><code>PureOSQP.BlockDiagonal</code></a></td><td style="text-align:right;"><a href="/PureOSQP.jl/dev/api#PureOSQP.BlockReduced"><code>PureOSQP.BlockReduced</code></a></td><td style="text-align:right;"><code>R</code> decouples into <code>K</code> independent systems</td></tr><tr><td style="text-align:right;"><a href="/PureOSQP.jl/dev/api#PureOSQP.RowCoupled"><code>PureOSQP.RowCoupled</code></a></td><td style="text-align:right;"><a href="/PureOSQP.jl/dev/api#PureOSQP.DiagonalLowRank"><code>PureOSQP.DiagonalLowRank</code></a></td><td style="text-align:right;"><code>R</code> is a diagonal plus a rank-<code>k</code> correction, solved by Woodbury</td></tr><tr><td style="text-align:right;"><a href="/PureOSQP.jl/dev/api#PureOSQP.KroneckerOperator"><code>PureOSQP.KroneckerOperator</code></a></td><td style="text-align:right;"><a href="/PureOSQP.jl/dev/api#PureOSQP.KroneckerReduced"><code>PureOSQP.KroneckerReduced</code></a></td><td style="text-align:right;"><code>R</code> is diagonal in the factors&#39; eigenbasis</td></tr></tbody></table><p><code>docs/src/examples.md</code> runs all three. The Kronecker one is the cautionary case: its backend applies only when <code>P</code> is a scalar multiple of <code>I</code>, <code>ρ</code> is one number, and <code>scaling = 0</code>, so most of its design is the rung that declines. A backend that would return a wrong answer outside its conditions has to check them, and checking them is the work.</p><h2 id="What-to-implement,-in-order" tabindex="-1">What to implement, in order <a class="header-anchor" href="#What-to-implement,-in-order" aria-label="Permalink to &quot;What to implement, in order {#What-to-implement,-in-order}&quot;">​</a></h2><p>Each step buys something on its own, so a representation is usable before it is finished.</p><h3 id="1.-size,-mul!-and-getindex-—-the-operator-works" tabindex="-1">1. <code>size</code>, <code>mul!</code> and <code>getindex</code> — the operator works <a class="header-anchor" href="#1.-size,-mul!-and-getindex-—-the-operator-works" aria-label="Permalink to &quot;1. \`size\`, \`mul!\` and \`getindex\` — the operator works {#1.-size,-mul!-and-getindex-—-the-operator-works}&quot;">​</a></h3><p>Declare the type <code>&lt;: AbstractMatrix{T}</code> and give it <code>size</code>, <code>mul!</code> against a vector, and <code>mul!</code> against a vector for its adjoint. Products run off whatever the type stores.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Blocks{T} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> AbstractMatrix{T}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    blocks</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Matrix{T}}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Base</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(A</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Blocks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(b </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(b, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), A</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">blocks), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(b </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(b, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), A</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">blocks))</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LinearAlgebra</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mul!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AbstractVector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, A</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Blocks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AbstractVector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    r </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> c </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> A</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">blocks</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        rows, cols </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(b)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        mul!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">view</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(y, r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(r </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rows </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)), b, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">view</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x, c</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(c </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cols </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        r </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rows</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        c </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cols</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    end</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> y</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p><code>getindex</code> is needed too, and it is easy to miss why: <code>setup</code> tests <code>P</code> for symmetry and for <code>P + σI</code> being positive definite <em>before</em> any backend is chosen, and the generic methods for both read entries. An operator with <code>size</code> and <code>mul!</code> alone fails there with a <code>CanonicalIndexError</code>, not at solve time. Two ways past it — give the type a <code>getindex</code>, which for most structured representations is a cheap lookup, or override <a href="/PureOSQP.jl/dev/api#PureOSQP.is_symmetric"><code>PureOSQP.is_symmetric</code></a> and <a href="/PureOSQP.jl/dev/api#PureOSQP.is_convex"><code>PureOSQP.is_convex</code></a> block-wise and declare <a href="/PureOSQP.jl/dev/api#PureOSQP.is_materializable"><code>PureOSQP.is_materializable</code></a> <code>false</code>.</p><p>With that, the solver runs and the per-iteration products cost what the structure costs. Selection still reaches the dense terminal, so an <code>n×n</code> reduced matrix is still formed.</p><p>An operator from a hierarchy that is not <code>AbstractMatrix</code> — <code>LinearMaps.LinearMap</code>, <code>SciMLOperators.AbstractSciMLOperator</code> — is wrapped rather than rewritten: <a href="/PureOSQP.jl/dev/api#PureOSQP.ProductOperator"><code>PureOSQP.ProductOperator</code></a> presents one as an <code>AbstractMatrix</code>, and loading LinearMaps lets <code>setup</code> and <code>solve</code> take a <code>LinearMap</code> directly.</p><h3 id="2.-structural_rows-—-setup-stops-paying-for-the-zeros" tabindex="-1">2. <code>structural_rows</code> — setup stops paying for the zeros <a class="header-anchor" href="#2.-structural_rows-—-setup-stops-paying-for-the-zeros" aria-label="Permalink to &quot;2. \`structural_rows\` — setup stops paying for the zeros {#2.-structural_rows-—-setup-stops-paying-for-the-zeros}&quot;">​</a></h3><p><a href="/PureOSQP.jl/dev/api#PureOSQP.structural_rows"><code>PureOSQP.structural_rows</code></a><code>(M, j)</code> answers which rows column <code>j</code> can hold a nonzero in. Equilibration and the dense formation both walk columns through it, and its generic answer is <em>every</em> row — so without a method, setup costs <code>O(mn)</code> however little the type stores.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PureOSQP</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">structural_rows</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(A</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Blocks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, j</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rowrange_of_the_block_holding</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(A, j)</span></span></code></pre></div><p>This is the highest-value method on the page. Adding it for <code>BandedMatrix</code> took a banded <code>setup</code> at <code>n = 2000</code> from 170 ms to 2.7 ms, because equilibration had been visiting every row of every column of a matrix holding <code>O(nb)</code> entries.</p><p>An operator with no columns to enumerate — one that only multiplies — has two other routes: build it with <code>probe = true</code>, which recovers column <code>j</code> as <code>op * eⱼ</code>, or pass <code>scaling = 0</code> and skip equilibration. Both are described under <a href="/PureOSQP.jl/dev/api#PureOSQP.ProductOperator"><code>PureOSQP.ProductOperator</code></a>.</p><h3 id="3.-A-LinearSystem-—-the-reduced-matrix-is-never-formed" tabindex="-1">3. A <code>LinearSystem</code> — the reduced matrix is never formed <a class="header-anchor" href="#3.-A-LinearSystem-—-the-reduced-matrix-is-never-formed" aria-label="Permalink to &quot;3. A \`LinearSystem\` — the reduced matrix is never formed {#3.-A-LinearSystem-—-the-reduced-matrix-is-never-formed}&quot;">​</a></h3><p>Subtype <a href="/PureOSQP.jl/dev/api#PureOSQP.LinearSystem"><code>PureOSQP.LinearSystem</code></a> and implement <code>factorize!</code>, <code>solve_system!</code> and <code>backend_info</code>; the contract is enforced at precompilation, and <a href="/PureOSQP.jl/dev/api#PureOSQP.refactor_rho!"><code>PureOSQP.refactor_rho!</code></a> is optional. The backend decides what &quot;solve the reduced system&quot; means for this structure, and it is where the <code>O(n²)</code> object stops existing.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">mutable struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> BlockSolve{T} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PureOSQP.LinearSystem</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    inv</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Matrix{T}}</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # one inverse per block; no n×n anything</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p><code>factorize!</code> builds whatever the structure implies — for blocks, one small factorization each; for a low-rank correction, a <code>k×k</code> capacitance. <code>solve_system!</code> calls <a href="/PureOSQP.jl/dev/api#PureOSQP.reduced_rhs!"><code>PureOSQP.reduced_rhs!</code></a> first, writes <code>ws.xtilde</code>, then <code>mul_A!</code> into <code>ws.ztilde</code>.</p><p>Two things worth copying from the shipped backends rather than rediscovering. Invert each block and use <code>symv</code> instead of keeping a factor and calling <code>ldiv!</code>: both cost <code>2nᵢ²</code> flops, but a triangular solve computes its entries in sequence and <code>symv</code> does not, and the <code>σI</code> in the reduced matrix bounds the conditioning that would otherwise make inverting unwise. And assemble each block with one <code>mul!</code> rather than a scalar loop — the difference was 1.7× on the block backend.</p><h3 id="4.-choose_backend-—-selection-finds-it" tabindex="-1">4. <code>choose_backend</code> — selection finds it <a class="header-anchor" href="#4.-choose_backend-—-selection-finds-it" aria-label="Permalink to &quot;4. \`choose_backend\` — selection finds it {#4.-choose_backend-—-selection-finds-it}&quot;">​</a></h3><p>Selection is multiple dispatch. A method on <code>choose_backend</code> for your pair of types wins over the ladder by being more specific, which is the whole mechanism:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PureOSQP</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">choose_backend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        P</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Blocks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, A</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Blocks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, proto</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AbstractVector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, m</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        D, E, c, rho_vec, sigma</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    )</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">BlockSolve</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>The ten arguments are the seam&#39;s signature, not decoration: <code>D</code>, <code>E</code>, <code>c</code> and <code>rho_vec</code> are there because two rungs decide by factoring the <em>equilibrated</em> matrix and handing that factorization on. Define a method with fewer and it silently never dispatches — the ladder proceeds to the dense terminal and nothing reports a problem.</p><p>Return <code>(backend, false)</code> when the backend arrives unfactored, <code>(backend, true)</code> when it already carries a factorization of the current data. A rung inside the ladder instead returns <code>nothing</code> to decline; see <a href="/PureOSQP.jl/dev/api#PureOSQP.select_backend"><code>PureOSQP.select_backend</code></a> for the order and what each rung serves.</p><h2 id="What-each-omission-costs" tabindex="-1">What each omission costs <a class="header-anchor" href="#What-each-omission-costs" aria-label="Permalink to &quot;What each omission costs {#What-each-omission-costs}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">stopped after</th><th style="text-align:right;">products</th><th style="text-align:right;">equilibration</th><th style="text-align:right;">reduced matrix</th><th style="text-align:right;">selection</th></tr></thead><tbody><tr><td style="text-align:right;">1 — <code>size</code>, <code>mul!</code>, <code>getindex</code></td><td style="text-align:right;">structured</td><td style="text-align:right;"><code>O(mn)</code></td><td style="text-align:right;">formed, <code>n×n</code></td><td style="text-align:right;">dense terminal</td></tr><tr><td style="text-align:right;">2 — <code>structural_rows</code></td><td style="text-align:right;">structured</td><td style="text-align:right;"><code>O(nnz)</code></td><td style="text-align:right;">formed, <code>n×n</code></td><td style="text-align:right;">dense terminal</td></tr><tr><td style="text-align:right;">3 — a <code>LinearSystem</code></td><td style="text-align:right;">structured</td><td style="text-align:right;"><code>O(nnz)</code></td><td style="text-align:right;">never formed</td><td style="text-align:right;">needs step 4</td></tr><tr><td style="text-align:right;">4 — <code>choose_backend</code></td><td style="text-align:right;">structured</td><td style="text-align:right;"><code>O(nnz)</code></td><td style="text-align:right;">never formed</td><td style="text-align:right;">reaches your backend</td></tr></tbody></table><p>The equilibration column is not the whole of <code>setup</code>. <code>is_symmetric</code> and <code>is_convex</code> run before a backend is chosen and their generic methods are <code>O(n²)</code> and <code>O(n³)</code> — a type that does not override them pays that on every <code>setup</code> however cheap the rest is. <a href="/PureOSQP.jl/dev/api#PureOSQP.BlockDiagonal"><code>PureOSQP.BlockDiagonal</code></a> overrides both block-wise, and is worth reading for the shape.</p><h2 id="Paths-that-need-entries,-and-how-they-decline" tabindex="-1">Paths that need entries, and how they decline <a class="header-anchor" href="#Paths-that-need-entries,-and-how-they-decline" aria-label="Permalink to &quot;Paths that need entries, and how they decline {#Paths-that-need-entries,-and-how-they-decline}&quot;">​</a></h2><p>Polishing and the solution derivatives copy <code>P</code> and <code>A</code> into a dense factorization one entry at a time. An operator that cannot answer that declares <a href="/PureOSQP.jl/dev/api#PureOSQP.is_materializable"><code>PureOSQP.is_materializable</code></a> <code>false</code>, and those paths then throw a message naming the remedy rather than a <code>MethodError</code> from inside the copy. The rungs that would form a matrix decline it too, so <code>linsys = :auto</code> reaches the matrix-free backend instead of failing inside a factorization.</p><p>Conjugate gradients is the fallback for an operator with no structure to exploit, and it is a real fallback rather than a good one: on an ill-conditioned problem it can return an answer that is wrong rather than merely slow. A structured direct backend is the reason this page exists.</p>`,34)])])}const g=s(n,[["render",r]]);export{k as __pageData,g as default};
