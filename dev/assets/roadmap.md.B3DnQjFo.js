import{_ as t,o,c as a,ao as d}from"./chunks/framework.CWHsw1fv.js";const u=JSON.parse('{"title":"Roadmap","description":"","frontmatter":{},"headers":[],"relativePath":"roadmap.md","filePath":"roadmap.md","lastUpdated":null}'),i={name:"roadmap.md"};function r(s,e,c,n,l,p){return o(),a("div",null,[...e[0]||(e[0]=[d('<h1 id="Roadmap" tabindex="-1">Roadmap <a class="header-anchor" href="#Roadmap" aria-label="Permalink to &quot;Roadmap {#Roadmap}&quot;">​</a></h1><p>What the reference implementation does that PureOSQP does not. The list is derived from libosqp&#39;s public API — <code>osqp_api.c</code>, <code>osqp_api_types.h</code> and <code>osqp_api_constants.h</code> for 1.x, plus the 0.6.2 <code>osqp.c</code> surface — compared against this package&#39;s exports, <a href="/PureOSQP.jl/dev/api#PureOSQP.Settings"><code>Settings</code></a> and <a href="/PureOSQP.jl/dev/api#PureOSQP.Solution"><code>Solution</code></a>.</p><p>Some entries are deliberate and will stay; those are marked as such and explained under <a href="/PureOSQP.jl/dev/attribution#What-is-deliberately-different">What is deliberately different</a>. The rest are open.</p><h2 id="Known-defect" tabindex="-1">Known defect <a class="header-anchor" href="#Known-defect" aria-label="Permalink to &quot;Known defect {#Known-defect}&quot;">​</a></h2><p><strong><code>verbose</code> is accepted but never read.</strong> The setting is validated and stored, and nothing in the solver consults it, so <code>verbose = true</code> silently does nothing. A setting that looks supported and is not is worse than an absent one. Either implement progress reporting or reject the keyword.</p><h2 id="Missing-capabilities" tabindex="-1">Missing capabilities <a class="header-anchor" href="#Missing-capabilities" aria-label="Permalink to &quot;Missing capabilities {#Missing-capabilities}&quot;">​</a></h2><p><strong>Solution derivatives.</strong> Upstream computes derivatives of the solution with respect to <code>P</code>, <code>q</code>, <code>A</code>, <code>l</code> and <code>u</code> (<code>osqp_adjoint_derivative_compute</code>, <code>..._get_mat</code>, <code>..._get_vec</code>), which is what lets a QP sit inside a differentiable program as a layer. Nothing here corresponds to it. This is the largest capability gap.</p><p><strong>Time limit and interruption.</strong> The solver does no timing at all, so there is no <code>time_limit</code> setting, no <code>TIME_LIMIT_REACHED</code> status, no interrupt checking and no <code>SIGINT</code> status. A run that takes <code>max_iter = 20000</code> iterations cannot be bounded by wall-clock.</p><p><strong>Code generation.</strong> <code>osqp_codegen</code> emits a self-contained C solver for a fixed problem structure, with embedded-mode, float-type and printing/profiling/interrupt toggles. The comparable capability here is <code>juliac --trim</code>, which the reference has no equivalent of, but the two are not interchangeable: trimming produces a Julia binary, not embeddable C.</p><p><strong>Indirect (matrix-free) solve.</strong> The whole conjugate-gradient path is absent: <code>cg_max_iter</code>, <code>cg_tol_reduction</code>, <code>cg_tol_fraction</code> and the diagonal preconditioner. Every backend here factors an explicit matrix, so a problem that can only supply matrix-vector products cannot be solved.</p><p><strong>Duality-gap termination.</strong> libosqp 1.x can terminate on the duality gap (<code>check_dualgap</code>) in addition to the primal and dual residuals. Only the residual tests are implemented.</p><p><strong><code>ρ</code> adaptation on KKT error.</strong> Upstream offers four modes: disabled, fixed iteration interval, wall-clock fraction, and relative KKT-error decrease. This package implements the first two. The wall-clock mode is <em>deliberately</em> omitted — it makes iteration counts depend on the machine, and reproducible counts are what the oracle tests check. The KKT-error mode carries no such problem and is simply not built.</p><h2 id="Missing-reported-information" tabindex="-1">Missing reported information <a class="header-anchor" href="#Missing-reported-information" aria-label="Permalink to &quot;Missing reported information {#Missing-reported-information}&quot;">​</a></h2><p><a href="/PureOSQP.jl/dev/api#PureOSQP.Solution"><code>Solution</code></a> reports <code>x</code>, <code>y</code>, <code>status</code>, <code>obj_val</code>, <code>prim_res</code>, <code>dual_res</code>, <code>iter</code> and <code>polished</code>, plus the infeasibility certificates. <code>OSQPInfo</code> also carries:</p><table tabindex="0"><thead><tr><th style="text-align:right;">field</th><th style="text-align:right;">note</th></tr></thead><tbody><tr><td style="text-align:right;"><code>dual_obj_val</code>, <code>duality_gap</code></td><td style="text-align:right;">the test referee computes the gap from problem data; the solver never reports it</td></tr><tr><td style="text-align:right;"><code>rho_estimate</code>, <code>rho_updates</code></td><td style="text-align:right;"><code>ws.refactor_count</code> conflates <code>ρ</code> updates with data-driven refactorizations, so neither can be recovered from it</td></tr><tr><td style="text-align:right;"><code>setup_time</code>, <code>solve_time</code>, <code>update_time</code>, <code>polish_time</code>, <code>run_time</code></td><td style="text-align:right;">no timing is collected</td></tr><tr><td style="text-align:right;"><code>rel_kkt_error</code>, <code>primdual_int</code></td><td style="text-align:right;">1.x convergence diagnostics</td></tr><tr><td style="text-align:right;"><code>status_polish</code></td><td style="text-align:right;">upstream distinguishes five polish outcomes; <code>polished::Bool</code> cannot separate &quot;no active set found, skipped&quot; from &quot;attempted and failed&quot;</td></tr></tbody></table><h2 id="Missing-settings" tabindex="-1">Missing settings <a class="header-anchor" href="#Missing-settings" aria-label="Permalink to &quot;Missing settings {#Missing-settings}&quot;">​</a></h2><ul><li><p><code>scaled_termination</code> — the scaled primal residual is already computed for the <code>ρ</code> rule, but termination always uses the unscaled residuals.</p></li><li><p><code>rho_is_vec</code> — <code>ρ</code> is always a vector here; upstream can run it as a scalar.</p></li><li><p><code>linsys_solver</code> selection across QDLDL, MKL Pardiso and CUDA backends. The equivalent choice here is <code>linsys = :auto | :kkt</code>, which selects a formulation rather than a library.</p></li><li><p><code>device</code>, <code>profiler_level</code>, <code>allocate_solution</code> — embedded and GPU concerns with no counterpart.</p></li></ul><h2 id="Missing-API-surface" tabindex="-1">Missing API surface <a class="header-anchor" href="#Missing-API-surface" aria-label="Permalink to &quot;Missing API surface {#Missing-API-surface}&quot;">​</a></h2><ul><li><p><code>osqp_update_settings</code> — settings are fixed once <a href="/PureOSQP.jl/dev/api#PureOSQP.setup"><code>setup</code></a> returns. 0.6.2 exposed about fifteen individual updaters (<code>osqp_update_max_iter</code>, <code>_eps_abs</code>, <code>_alpha</code>, <code>_polish</code>, …) which 1.x consolidated into one call. Note that changing <code>rho</code> or <code>sigma</code> requires a refactorization, so this is not merely a field assignment.</p></li><li><p><code>osqp_update_rho</code> as a public entry point.</p></li><li><p><code>cold_start!</code> exists but is not exported, while <code>warm_start!</code> is.</p></li><li><p>Introspection: <code>osqp_version</code>, <code>osqp_capabilities</code>, <code>osqp_error_message</code>, <code>osqp_get_dimensions</code>.</p></li></ul><p><a href="/PureOSQP.jl/dev/api#PureOSQP.update!"><code>update!</code></a> does cover upstream&#39;s whole data-update family — <code>osqp_update_lin_cost</code>, <code>osqp_update_bounds</code>, <code>osqp_update_lower_bound</code>, <code>osqp_update_upper_bound</code>, <code>osqp_update_P</code>, <code>osqp_update_A</code> and <code>osqp_update_P_A</code> — and <code>warm_start!(; x, y)</code> covers <code>osqp_warm_start_x</code> and <code>osqp_warm_start_y</code>.</p><h2 id="Ecosystem" tabindex="-1">Ecosystem <a class="header-anchor" href="#Ecosystem" aria-label="Permalink to &quot;Ecosystem {#Ecosystem}&quot;">​</a></h2><p><strong>No MathOptInterface wrapper.</strong> In practice this is a larger barrier to use than any algorithmic gap above, since it is what makes a solver reachable from JuMP.</p><p><strong>No sparse linear-algebra backend.</strong> Deliberate, and measured: forming the reduced matrix densifies, and a sparse factorization of it does not pay. See <a href="/PureOSQP.jl/dev/benchmarks#How-the-sparsest-case-was-closed">How the sparsest case was closed</a> for the fill-in measurements. A <code>SparseArrays</code> extension does specialise equilibration&#39;s column traversals.</p><h2 id="Suggested-order" tabindex="-1">Suggested order <a class="header-anchor" href="#Suggested-order" aria-label="Permalink to &quot;Suggested order {#Suggested-order}&quot;">​</a></h2><p>The small items close real holes for little work: fix or reject <code>verbose</code>, export <code>cold_start!</code>, add <code>time_limit</code>, separate <code>ρ</code> updates from refactorizations in the reported counts, and report <code>duality_gap</code> and a real polish status. Derivatives and a MathOptInterface wrapper are projects rather than fixes.</p>',25)])])}const m=t(i,[["render",r]]);export{u as __pageData,m as default};
