import{_ as t,o as a,c as o,ao as i}from"./chunks/framework.BAh_9QZx.js";const g=JSON.parse('{"title":"Guarantees","description":"","frontmatter":{},"headers":[],"relativePath":"guarantees.md","filePath":"guarantees.md","lastUpdated":null}'),r={name:"guarantees.md"};function d(l,e,n,s,c,h){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="Guarantees" tabindex="-1">Guarantees <a class="header-anchor" href="#Guarantees" aria-label="Permalink to &quot;Guarantees {#Guarantees}&quot;">​</a></h1><p>This page gives the evidence for three claims about PureOSQP, and says where each one stops.</p><table tabindex="0"><thead><tr><th style="text-align:right;">claim</th><th style="text-align:right;">meaning</th><th style="text-align:right;">limit</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>no allocation on the hot path</strong></td><td style="text-align:right;">No memory allocation occurs during iterations, avoiding garbage collection.</td><td style="text-align:right;">Guaranteed for <code>Vector</code>-backed workspaces. Not claimed for custom operators.</td></tr><tr><td style="text-align:right;"><strong>type stability</strong></td><td style="text-align:right;">Performance is consistent because there is no hidden dynamic dispatch.</td><td style="text-align:right;">Checked for all reachable backends.</td></tr><tr><td style="text-align:right;"><strong>compiles under <code>juliac --trim</code></strong></td><td style="text-align:right;">Can be built into a standalone binary with no Julia runtime.</td><td style="text-align:right;">Entry points are enumerated; sparse operands require a named backend.</td></tr></tbody></table><p>All claims are machine-checked.</p><h2 id="The-LinearSystem-contract" tabindex="-1">The <code>LinearSystem</code> contract <a class="header-anchor" href="#The-LinearSystem-contract" aria-label="Permalink to &quot;The `LinearSystem` contract {#The-LinearSystem-contract}&quot;">​</a></h2><p>The factorization backend is a declared interface. <a href="https://github.com/el-oso/TypeContracts.jl" target="_blank" rel="noreferrer">TypeContracts.jl</a> enforces it at precompilation.</p><p><strong>Mandatory methods</strong></p><ul><li><p><code>factorize!(::Self, ::Problem, ::SystemWeights) :: Bool</code></p></li><li><p><code>solve_system!(::Self, ::Problem, ::SystemWeights, ::Any, ::Any, ::Any, ::Any) :: Nothing</code></p></li><li><p><code>backend_info(::Self) :: BackendInfo</code></p></li></ul><p><strong>Optional methods</strong></p><ul><li><p><code>refactor_weights!(::Self, ::Problem, ::SystemWeights) :: Bool</code> — refresh after only the weights changed</p></li><li><p><code>solve_multiplier!(::Self, ::Problem, ::SystemWeights, ::Any, ::Any, ::Any, ::Any) :: Nothing</code> — solve for <code>x</code> and the multiplier <code>ν</code></p></li><li><p><code>check_update(::Self, ::Any, ::Any) :: Nothing</code> — throw unless the backend can serve the replacement <code>P</code> and <code>A</code></p></li><li><p><code>set_tolerance_level!(::Self, ::Any) :: Nothing</code> — the residual level an inexact backend&#39;s next solves are relative to</p></li><li><p><code>set_refresh_index!(::Self, ::Int) :: Nothing</code> — the refresh index the next preconditioner update receives</p></li><li><p><code>adopt_settings!(::Self, ::QPAlgorithm, ::Options) :: Nothing</code> — copy in the algorithm parameters and options the backend reads</p></li><li><p><code>use_residual_stop!(::Self, ::Bool) :: Nothing</code> — choose an iterative backend&#39;s inner stopping rule</p></li><li><p><code>last_solve_converged(::Self) :: Bool</code> — whether the most recent solve met its stopping test</p></li><li><p><code>inner_iterations(::Self) :: Int</code> — inner iterations spent over the backend&#39;s life</p></li></ul><p><code>PureQPBase.refactor_weights!</code> is not part of the contract, because its default is to rebuild the factorization.</p><p><code>@verify</code> checks at precompilation that a backend has the required methods and returns the right types.</p><p>To add a backend, subtype <code>LinearSystem</code>, write the mandatory methods, and use <code>@verify</code>. The backend is fixed when the workspace is built, so every per-iteration call dispatches statically.</p><h2 id="No-allocation,-no-type-instability" tabindex="-1">No allocation, no type instability <a class="header-anchor" href="#No-allocation,-no-type-instability" aria-label="Permalink to &quot;No allocation, no type instability {#No-allocation,-no-type-instability}&quot;">​</a></h2><p><code>bench/strictmode_audit.jl</code> uses <a href="https://github.com/el-oso/StrictMode.jl" target="_blank" rel="noreferrer">StrictMode.jl</a> to check that these functions are type-stable and allocation-free:</p><table tabindex="0"><thead><tr><th style="text-align:right;">function</th><th style="text-align:right;">guarantees</th></tr></thead><tbody><tr><td style="text-align:right;"><code>admm_step!</code></td><td style="text-align:right;">type-stable, allocation-free</td></tr><tr><td style="text-align:right;"><code>update_residuals!</code></td><td style="text-align:right;">type-stable, allocation-free</td></tr><tr><td style="text-align:right;"><code>solve_system!</code></td><td style="text-align:right;">type-stable, allocation-free</td></tr><tr><td style="text-align:right;"><code>check_termination</code></td><td style="text-align:right;">type-stable</td></tr><tr><td style="text-align:right;"><code>factorize!</code></td><td style="text-align:right;">type-stable</td></tr><tr><td style="text-align:right;"><code>refactor_weights!</code></td><td style="text-align:right;">type-stable</td></tr><tr><td style="text-align:right;"><code>solve!</code></td><td style="text-align:right;">type-stable</td></tr></tbody></table><p>The interior-point method&#39;s own hot kernels carry the same guarantees. We check them over every backend its candidate list reaches: <code>FullKKT</code>, the sparse KKT family, the structured reduced backends, and <code>:indirect</code> with a caller preconditioner.</p><table tabindex="0"><thead><tr><th style="text-align:right;">function</th><th style="text-align:right;">guarantees</th></tr></thead><tbody><tr><td style="text-align:right;"><code>ipm_step!</code></td><td style="text-align:right;">type-stable, allocation-free</td></tr><tr><td style="text-align:right;"><code>ipm_residuals!</code></td><td style="text-align:right;">type-stable, allocation-free</td></tr><tr><td style="text-align:right;"><code>solve_multiplier!</code></td><td style="text-align:right;">type-stable, allocation-free</td></tr><tr><td style="text-align:right;"><code>check_termination</code></td><td style="text-align:right;">type-stable</td></tr><tr><td style="text-align:right;"><code>factorize!</code></td><td style="text-align:right;">type-stable</td></tr><tr><td style="text-align:right;"><code>refactor_weights!</code></td><td style="text-align:right;">type-stable</td></tr><tr><td style="text-align:right;"><code>solve!</code></td><td style="text-align:right;">type-stable</td></tr></tbody></table><p>Two notes:</p><ul><li><p>A sparse arithmetic backend can allocate inside its own library. That covers <code>factorize!</code>, <code>refactor_weights!</code> and <code>solve!</code> on the sparse KKT family under both algorithms. <code>solve_multiplier!</code> there is this package&#39;s own code, so it keeps the full guarantee.</p></li><li><p>We check the matrix-free backend by measurement, because its static analysis shows branches that are possible but never taken. Under the interior-point method, the <code>try</code>/<code>catch</code> that turns a non-positive-definite preconditioner into a missed solve, rather than an exception, sits in a helper outside the audited kernel, so it does not change what we measure.</p></li></ul><p>An operator you supply is only as fast as its own <code>mul!</code> method.</p><h2 id="trim-compatibility" tabindex="-1"><code>--trim</code> compatibility <a class="header-anchor" href="#trim-compatibility" aria-label="Permalink to &quot;`--trim` compatibility {#trim-compatibility}&quot;">​</a></h2><p><code>juliac --trim</code> needs every function call resolved statically.</p><p><strong>We check every backend.</strong> <code>PureQPBase/src/PureQPBase.jl</code> uses <code>@verify LinearSystem subtypes = true trim_compat = true</code>, and each solver package does the same for the workspace and algorithm it defines. The check runs during precompilation, for every subtype.</p><p><strong>We list the entry points.</strong> <code>PureOSQP/test/trim_tests.jl</code> validates concrete calls for every public path:</p><ul><li><p><code>solve</code> with various settings and sparse or dense operands.</p></li><li><p>Every structured representation: diagonal, tridiagonal, banded, low-rank, block-diagonal, Kronecker, and products.</p></li><li><p><code>setup</code> → <code>solve!</code> → <code>update!</code> → <code>solve!</code>, and similar sequences with <code>warm_start!</code>.</p></li><li><p><code>update_settings!</code>, <code>update_rho!</code>, and <code>cold_start!</code>.</p></li><li><p>The derivatives.</p></li><li><p><code>InteriorPoint()</code> on <code>FullKKT</code> (its default and its named KKT backend), the sparse KKT family, a diagonal pair, <code>:indirect</code> with a caller preconditioner on both a matrix pair and a <code>ProductOperator</code> pair, and a <code>setup</code> → <code>solve!</code> → <code>update!</code> → <code>solve!</code> sequence and the derivatives, each under <code>InteriorPoint()</code>.</p></li></ul><p>For a sparse problem you must name the backend — <code>:kkt</code>, <code>:dense</code> or <code>:indirect</code> — to stay trim-compatible.</p>',27)])])}const y=t(r,[["render",d]]);export{g as __pageData,y as default};
