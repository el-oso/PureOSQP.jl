import{_ as t,o as i,c as a,ao as o}from"./chunks/framework.BAh_9QZx.js";const g=JSON.parse('{"title":"Choosing an algorithm","description":"","frontmatter":{},"headers":[],"relativePath":"algorithms.md","filePath":"algorithms.md","lastUpdated":null}'),s={name:"algorithms.md"};function r(n,e,d,l,h,c){return i(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="Choosing-an-algorithm" tabindex="-1">Choosing an algorithm <a class="header-anchor" href="#Choosing-an-algorithm" aria-label="Permalink to &quot;Choosing an algorithm {#Choosing-an-algorithm}&quot;">​</a></h1><p>Three algorithms solve the same problem, and they come from three packages. PureOSQP.jl supplies <a href="/PureQP.jl/dev/api#PureOSQP.OperatorSplitting"><code>OperatorSplitting</code></a>. PureIPM.jl supplies <a href="/PureQP.jl/dev/api#PureIPM.InteriorPoint"><code>InteriorPoint</code></a>. PureDAQP.jl supplies <a href="/PureQP.jl/dev/api#PureDAQP.ActiveSet"><code>ActiveSet</code></a>. Each re-exports PureQPBase.jl, which holds what they share, so <code>using</code> any one is enough to solve a problem. <code>using</code> several puts all their algorithms on the same <a href="/PureQP.jl/dev/api#PureQPBase.solve"><code>solve</code></a>.</p><p><code>OperatorSplitting</code> and <code>InteriorPoint</code> are the pair that share the matrix support and the linear-system backends: either one takes a sparse, structured or matrix-free <code>P</code> and <code>A</code> and chooses a backend for it. <code>ActiveSet</code> reads dense matrices only and maintains its own factorization, so most of what the two have in common does not apply to it. Each section below says which algorithms it is about.</p><p>The sixth argument of <a href="/PureQP.jl/dev/api#PureQPBase.solve"><code>solve</code></a> and <a href="/PureQP.jl/dev/api#PureQPBase.setup"><code>setup</code></a> picks the algorithm. It also holds the settings only that algorithm reads. Everything shared is a keyword argument of <a href="/PureQP.jl/dev/api#PureQPBase.Options"><code>Options</code></a>. The five-argument form runs <a href="/PureQP.jl/dev/api#PureOSQP.OperatorSplitting"><code>OperatorSplitting</code></a>.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PureIPM</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ws </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> setup</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(P, q, A, l, u, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">InteriorPoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); max_iter </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sol </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> solve!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ws)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ws</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">algorithm     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># InteriorPoint{Float64, Float64, Float64, Int64}: parameters in the solve&#39;s element type</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ws</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">options       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Options{Float64}: max_iter, the tolerances, linsys, polishing, …</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">update_settings!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ws; eps_abs </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1e-10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># change an option</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">update_settings!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ws, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">InteriorPoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(reg_primal </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1e-6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># replace the algorithm parameters</span></span></code></pre></div><p>The keyword arguments are the fields of <a href="/PureQP.jl/dev/api#PureQPBase.Options"><code>Options</code></a>. The algorithms default some of them differently: <code>max_iter</code> is <code>4000</code> for <code>OperatorSplitting</code>, <code>100</code> for <code>InteriorPoint</code> and <code>1000</code> for <code>ActiveSet</code>, and the tolerances are <code>1e-3</code>, <code>1e-8</code> and <code>sqrt(eps)</code>. <a href="/PureQP.jl/dev/api#PureQPBase.default_options"><code>default_options</code></a> shows the full set for any of them. <code>ActiveSet</code> reads only <code>max_iter</code> from this set. It refuses <code>linsys</code>, <code>scaling</code> and <code>polishing</code> outright, and the rest do not reach it: its tolerances are its own parameters, <code>primal_tol</code> and <code>zero_tol</code>, because it prices normalized rows rather than measuring a residual against <code>eps_abs</code>, and it carries its working set across a re-solve whatever <code>warm_starting</code> says. A value you pass is always used as you gave it. A setting passed in the wrong place throws, and names where it belongs:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">InteriorPoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(rho </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)                           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># MethodError: rho is not an InteriorPoint parameter</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">solve</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(P, q, A, l, u; rho </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ArgumentError: rho is a parameter of OperatorSplitting</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">solve</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(P, q, A, l, u, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">InteriorPoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); rho </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ArgumentError: rho is not an option of InteriorPoint</span></span></code></pre></div><h2 id="Accuracy-and-iteration-count" tabindex="-1">Accuracy and iteration count <a class="header-anchor" href="#Accuracy-and-iteration-count" aria-label="Permalink to &quot;Accuracy and iteration count {#Accuracy-and-iteration-count}&quot;">​</a></h2><p><a href="/PureQP.jl/dev/api#PureOSQP.OperatorSplitting"><code>OperatorSplitting</code></a> is OSQP&#39;s ADMM iteration: many cheap iterations that share one factorization. <a href="/PureQP.jl/dev/api#PureIPM.InteriorPoint"><code>InteriorPoint</code></a> is a Mehrotra predictor–corrector method: a few iterations, each factoring a new Newton system. Both converge toward the solution, so for both the question is how many iterations a given tolerance costs. The measurements below are of those two.</p><p><a href="/PureQP.jl/dev/api#PureDAQP.ActiveSet"><code>ActiveSet</code></a> does not belong on that axis. It terminates finitely: each iteration adds or drops one row, and when no row prices in it returns the exact solution of the equality-constrained QP over the rows that are active, to the accuracy of one triangular solve. There is no tolerance to tighten and no accuracy to trade against iteration count. What decides its cost is how many rows enter the working set before it settles, which is a property of the problem rather than of a setting.</p><p><code>PureIPM/bench/ipm_vs_clarabel.jl</code> runs both on the smallest instance of each OSQP suite problem class, <code>InteriorPoint</code> at <code>eps_abs = eps_rel = 1e-8</code> and <code>OperatorSplitting</code> at <code>1e-6</code>, the tightest tolerance ADMM reaches in a modest iteration count on these problems (<code>PureIPM/bench/results/ipm_vs_clarabel.json</code>):</p><table tabindex="0"><thead><tr><th style="text-align:right;">class</th><th style="text-align:right;">ADMM iterations (<code>1e-6</code>)</th><th style="text-align:right;">IPM iterations (<code>1e-8</code>)</th></tr></thead><tbody><tr><td style="text-align:right;">Random QP</td><td style="text-align:right;">200</td><td style="text-align:right;">9</td></tr><tr><td style="text-align:right;">Eq QP</td><td style="text-align:right;">50</td><td style="text-align:right;">2</td></tr><tr><td style="text-align:right;">Portfolio</td><td style="text-align:right;">125</td><td style="text-align:right;">10</td></tr><tr><td style="text-align:right;">Lasso</td><td style="text-align:right;">100</td><td style="text-align:right;">6</td></tr><tr><td style="text-align:right;">SVM</td><td style="text-align:right;">375</td><td style="text-align:right;">9</td></tr><tr><td style="text-align:right;">Huber</td><td style="text-align:right;">125</td><td style="text-align:right;">9</td></tr><tr><td style="text-align:right;">Control</td><td style="text-align:right;">50</td><td style="text-align:right;">7</td></tr></tbody></table><p><code>InteriorPoint</code> reaches a tighter tolerance in 2 to 10 outer iterations. <code>OperatorSplitting</code> takes 50 to 375 at a looser one on the same problems. The gap is not a fixed offset. Run the benchmark suite&#39;s full-size problems through <a href="/PureQP.jl/dev/benchmarks#The-OSQP-benchmark-suite"><code>PureOSQP/bench/osqp_suite.jl</code></a> at <code>eps_abs = eps_rel = 1e-5</code> and through <a href="/PureQP.jl/dev/benchmarks#The-ρ-schedule"><code>PureOSQP/bench/rho_schedule.jl</code></a> at <code>1e-6</code>, and every class that changes at all takes more ADMM iterations at the tighter tolerance: Random QP 925 → 1225, Portfolio 450 → 600, Lasso 100 → 125, SVM 300 → 325, Control 325 → 450. What sets <code>InteriorPoint</code>&#39;s iteration count is Newton&#39;s method converging locally, which cares far less about how tight the tolerance is. <code>OperatorSplitting</code> is a first-order method, which cares a lot.</p><p>This does not say <code>InteriorPoint</code> is always faster. Each of its iterations costs a factorization, while ADMM&#39;s iterations only apply the one it already has, so where they cross depends on the problem. <a href="/PureQP.jl/dev/benchmarks#The-interior-point-method-against-Clarabel">Benchmarks</a> has the times, not just the iteration counts, and <a href="/PureQP.jl/dev/benchmarks#Against-other-solvers">Against other solvers</a> times all three on the same problems.</p><h2 id="Re-solving-a-sequence" tabindex="-1">Re-solving a sequence <a class="header-anchor" href="#Re-solving-a-sequence" aria-label="Permalink to &quot;Re-solving a sequence {#Re-solving-a-sequence}&quot;">​</a></h2><p>All three accept <a href="/PureQP.jl/dev/api#PureQPBase.update!"><code>update!</code></a>, <a href="/PureQP.jl/dev/api#PureQPBase.warm_start!"><code>warm_start!</code></a>, <a href="/PureQP.jl/dev/api#PureQPBase.cold_start!"><code>cold_start!</code></a> and <a href="/PureQP.jl/dev/api#PureQPBase.update_settings!"><code>update_settings!</code></a>. <code>OperatorSplitting</code> and <code>InteriorPoint</code> start a re-solve from the previous point when <code>warm_starting = true</code> (the default); <code>ActiveSet</code> restarts from the previous working set instead, which <a href="/PureQP.jl/dev/api#PureQPBase.cold_start!"><code>cold_start!</code></a> is what drops.</p><p><strong><code>OperatorSplitting</code> can skip the factorization altogether.</strong> Updating <code>q</code> alone never refactorizes. Updating <code>l</code> or <code>u</code> refactorizes only when a row moves between equality, inequality and free. Updating <code>P</code> or <code>A</code> always does. The Model Predictive Control loop in <a href="/PureQP.jl/dev/examples#Model-predictive-control">Examples</a> is built on this: fifteen closed-loop solves, one factorization, because only the initial-state bounds move and none of them cross a class. Each solve is short as well, because it starts from the previous step&#39;s iterates. That is what the warm start buys under ADMM: fewer iterations on top of no refactorization.</p><p><strong><code>InteriorPoint</code> refactors every outer iteration, whatever <code>update!</code> did.</strong> Every iteration solves a fresh Newton system at that iteration&#39;s row weights, so there is no factorization for <code>update!</code> to keep. It saves the equilibration and the buffers, not a solve. <code>warm_start!</code> still seeds the first iterate from a point you supply, and a re-solve takes at most as many outer iterations as a cold one (<code>PureIPM/test/ipm_tests.jl</code> checks that). But there is little to save. The count is already 2 to 10 at the default tolerance, so a warm start shortens a run that was already short. It does not replace hundreds of iterations with dozens.</p><p><strong><code>ActiveSet</code> keeps the working set, and hands back the same <code>Solution</code> every time.</strong> Updating <code>q</code>, <code>l</code> or <code>u</code> keeps the Cholesky factor of <code>P</code> and the transformed constraint matrix, so only the right-hand side is rebuilt; updating <code>P</code> or <code>A</code> rebuilds the reduction. The re-solve starts from the previous answer&#39;s active rows, which is the whole of what a warm start means here.</p><div class="warning custom-block"><p class="custom-block-title">Warning</p><p>The <code>Solution</code> an <code>ActiveSet</code> solve returns is the workspace&#39;s own object, and its <code>x</code> and <code>y</code> are the workspace&#39;s own arrays. That is what makes a solve allocate nothing, and it means a result held across the next <code>solve!</code> is overwritten in place. Copy what you need before re-solving. The other two algorithms return a fresh <code>Solution</code> each time.</p></div><h2 id="What-each-algorithm-throws-on" tabindex="-1">What each algorithm throws on <a class="header-anchor" href="#What-each-algorithm-throws-on" aria-label="Permalink to &quot;What each algorithm throws on {#What-each-algorithm-throws-on}&quot;">​</a></h2><p><code>ActiveSet</code> takes dense <code>P</code> and <code>A</code> and nothing else: the reduction forms <code>A R⁻¹</code>, which an operator cannot supply and which is dense whatever <code>A</code> was, so a matrix it cannot read entry by entry throws at <a href="/PureQP.jl/dev/api#PureQPBase.setup"><code>setup</code></a>. The other two limit none of the matrix types in <a href="./matrices">Matrix types</a> or <a href="/PureQP.jl/dev/operators#Structured-operators">Structured operators</a> beyond what <code>linsys</code> asks for. They differ in what an operator you supply needs, and in which <code>linsys</code> backends each one accepts.</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;"><code>OperatorSplitting</code></th><th style="text-align:right;"><code>InteriorPoint</code></th><th style="text-align:right;"><code>ActiveSet</code></th></tr></thead><tbody><tr><td style="text-align:right;">matrix-free operators (<code>linsys = :indirect</code>)</td><td style="text-align:right;">works with the built-in Jacobi preconditioner, or none</td><td style="text-align:right;">needs <code>linsys = :indirect</code>, a <strong>caller-supplied</strong> preconditioner, and <code>scaling = 0</code>; passing the built-in preconditioners or equilibration throws, naming the remedy (<a href="/PureQP.jl/dev/operators#Operators-under-the-interior-point-method">Operators under the interior-point method</a>)</td><td style="text-align:right;">throws: the reduction needs entries</td></tr><tr><td style="text-align:right;"><code>linsys = :kronecker</code></td><td style="text-align:right;">works</td><td style="text-align:right;">throws: the Kronecker backend needs one weight for every row, and the interior-point method&#39;s weights are per-row</td><td style="text-align:right;">throws: no backend to select</td></tr><tr><td style="text-align:right;"><code>linsys = :lowrank</code></td><td style="text-align:right;">works</td><td style="text-align:right;">throws: the Woodbury solve misses the tolerance on linear programs (<a href="/PureQP.jl/dev/algorithm#Backends-under-the-interior-point-method">Algorithm</a>)</td><td style="text-align:right;">throws: no backend to select</td></tr><tr><td style="text-align:right;"><code>scaling</code></td><td style="text-align:right;">any value</td><td style="text-align:right;">any value</td><td style="text-align:right;">throws unless <code>0</code>: the reduction normalizes its own rows</td></tr><tr><td style="text-align:right;"><code>polishing = true</code></td><td style="text-align:right;">works</td><td style="text-align:right;">works, and is required before a derivative</td><td style="text-align:right;">throws: the answer is already exact over the working set</td></tr><tr><td style="text-align:right;">an indefinite <code>P</code></td><td style="text-align:right;">throws at <code>setup</code></td><td style="text-align:right;">throws at <code>setup</code></td><td style="text-align:right;">throws at <code>setup</code>, from the Cholesky of <code>P + eps_prox*I</code></td></tr></tbody></table><p>We measured why <code>InteriorPoint</code> needs a preconditioner of your own on an operator. We did not assume it. Its row weights reach <code>1/reg_dual</code>, <code>1e8</code> by default, on equality and active rows, and they change every outer iteration. A fixed diagonal preconditioner cannot keep conjugate gradients inside its budget at that spread, though it can under ADMM&#39;s fixed <code>ρ</code>. <code>PureIPM/bench/ipm_matrixfree.jl</code> measures this on 24 dense planted instances with a lagged Cholesky preconditioner the caller supplies, refreshed every third outer iteration. All 24 solve at <code>eps = 1e-6</code> with a referee residual of at most <code>7.9e-7</code>, in the same outer iterations as the dense full-KKT factorization (<code>PureIPM/bench/results/ipm_matrixfree.json</code>). One sparse instance with a limited-memory incomplete <code>LDLᵀ</code> preconditioner fails instead. A preconditioner must keep the inner iteration count bounded as the weights spread, and not every cheap one does.</p><h2 id="Polishing,-derivatives-and-infeasibility" tabindex="-1">Polishing, derivatives and infeasibility <a class="header-anchor" href="#Polishing,-derivatives-and-infeasibility" aria-label="Permalink to &quot;Polishing, derivatives and infeasibility {#Polishing,-derivatives-and-infeasibility}&quot;">​</a></h2><p><strong>Polishing runs the same way under <code>OperatorSplitting</code> and <code>InteriorPoint</code>.</strong> <code>polishing = true</code> guesses the active set from the iterate, solves the equality-constrained QP that comes out of it with <code>bunchkaufman!</code> and three steps of iterative refinement, and replaces the answer only if both residuals improve. The <code>polishing</code>, <code>polish_refine_iter</code> and <code>delta</code> options work with either of those two. <code>ActiveSet</code> refuses <code>polishing = true</code>: it already ends on an exact solution of the equality-constrained QP over its working set, which is what polishing computes.</p><p><strong>Derivatives need polishing under <code>InteriorPoint</code> only.</strong> <a href="/PureQP.jl/dev/api#PureQPBase.adjoint_derivative"><code>adjoint_derivative</code></a> and <a href="/PureQP.jl/dev/api#PureQPBase.forward_derivative"><code>forward_derivative</code></a> read the active set by asking which multipliers sit far from zero. ADMM projects its multipliers onto the feasible box directly, so an inactive row&#39;s multiplier is already at or near zero and there is nothing extra to check. <code>ActiveSet</code> puts every inactive row&#39;s multiplier at exactly zero, which is the same test&#39;s best case. An interior-point solution holds an inactive row&#39;s multiplier at the barrier parameter <code>μ_final</code> instead, and the active-set test cannot tell that apart from a truly active row. So taking a derivative from an unpolished <code>InteriorPointWorkspace</code> throws and asks for <code>polishing = true</code> first. Polishing brings that multiplier down before you take the derivative.</p><p><strong><code>OperatorSplitting</code> and <code>InteriorPoint</code> use the same infeasibility test.</strong> <code>InteriorPoint</code> has no primal- and dual-infeasibility check of its own. It reuses ADMM&#39;s certificate test on its own last step and its normalized iterate. Both report <code>PRIMAL_INFEASIBLE</code> and <code>DUAL_INFEASIBLE</code>, and their <code>*_INACCURATE</code> versions, with a certificate in <code>Solution.prim_inf_cert</code> or <code>Solution.dual_inf_cert</code>. <code>ActiveSet</code> finds primal infeasibility differently: a dual step along the null direction of a singular working-set Gram matrix with no row to block it is an unbounded dual ray, and the dual of a convex QP is unbounded exactly when the primal is infeasible. It reports <code>PRIMAL_INFEASIBLE</code> without populating either certificate field, and has no dual-infeasibility test and no <code>*_INACCURATE</code> status.</p><p><strong>What <code>Solution</code> carries differs in which fields read zero.</strong> The struct is shared, so every field exists under every algorithm. But <code>rho_estimate</code>, <code>rho_updates</code>, <code>accel_declined</code>, <code>primdual_int</code> and <code>primdual_int_log</code> are zero under <code>InteriorPoint</code> and under <code>ActiveSet</code>, because there is no <code>ρ</code>, no accelerator and no primal-dual integral to report. <code>cg_iters</code> is nonzero only under <code>linsys = :indirect</code>, which neither of those two accepts. <code>InteriorPoint</code> returns <code>NUMERICAL_ERROR</code> for a stalled Newton system, a non-finite residual, or conjugate gradients missing too many solves in a row; <code>ActiveSet</code> returns it when a dual step toward the working set&#39;s multipliers finds no row to block it through a nonsingular Gram matrix, which the method&#39;s own argument rules out and so signals a numerical breakdown. ADMM never reports it.</p><h2 id="Summary" tabindex="-1">Summary <a class="header-anchor" href="#Summary" aria-label="Permalink to &quot;Summary {#Summary}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;"><code>OperatorSplitting</code> (default)</th><th style="text-align:right;"><code>InteriorPoint</code></th><th style="text-align:right;"><code>ActiveSet</code></th></tr></thead><tbody><tr><td style="text-align:right;">iteration cost</td><td style="text-align:right;">many cheap iterations, one factorization reused until <code>ρ</code> changes</td><td style="text-align:right;">a few iterations, a fresh factorization each</td><td style="text-align:right;">a few iterations, each a rank-one update of the working set&#39;s <code>LDLᵀ</code></td></tr><tr><td style="text-align:right;">default tolerance</td><td style="text-align:right;"><code>1e-3</code></td><td style="text-align:right;"><code>1e-8</code></td><td style="text-align:right;">exact at the working set; <code>primal_tol</code> decides which rows enter</td></tr><tr><td style="text-align:right;">matrices</td><td style="text-align:right;">any <code>AbstractMatrix</code>, structure and sparsity exploited</td><td style="text-align:right;">the same</td><td style="text-align:right;">dense only, structure and sparsity ignored</td></tr><tr><td style="text-align:right;"><code>update!</code></td><td style="text-align:right;">can skip refactorization entirely (<code>q</code>-only updates always do)</td><td style="text-align:right;">refactorizes every outer iteration regardless</td><td style="text-align:right;">keeps the reduction unless <code>P</code> or <code>A</code> changes</td></tr><tr><td style="text-align:right;">matrix-free operators</td><td style="text-align:right;">no restriction</td><td style="text-align:right;">needs a caller-supplied preconditioner and <code>scaling = 0</code></td><td style="text-align:right;">not supported</td></tr><tr><td style="text-align:right;"><code>linsys</code></td><td style="text-align:right;">every backend</td><td style="text-align:right;">all but <code>:kronecker</code> and <code>:lowrank</code></td><td style="text-align:right;">none: it has no backend to choose</td></tr><tr><td style="text-align:right;">derivatives</td><td style="text-align:right;">ready from the iterate as it stands</td><td style="text-align:right;">require <code>polishing = true</code> first</td><td style="text-align:right;">ready: inactive multipliers are exactly zero</td></tr><tr><td style="text-align:right;">infeasibility certificates</td><td style="text-align:right;">yes</td><td style="text-align:right;">yes, through the same test</td><td style="text-align:right;">primal only, and without a certificate</td></tr></tbody></table><p>Pick <code>ActiveSet</code> for dense problems with few rows active at the solution, especially when you want the exact answer rather than one to a tolerance. Pick <code>InteriorPoint</code> when you need a tolerance tighter than ADMM reaches in a modest iteration count, or when you solve once rather than in a loop that reuses a factorization. Pick <code>OperatorSplitting</code> for repeated solves through <code>update!</code>, for sparse or structured data, and for matrix-free operators when you have no preconditioner of your own.</p><p><a href="/PureQP.jl/dev/benchmarks#The-interior-point-method-against-Clarabel">Benchmarks</a> and the suite tables above it give times, not just iteration counts. This page says which algorithm fits a given problem, not how many milliseconds it takes.</p>`,33)])])}const u=t(s,[["render",r]]);export{g as __pageData,u as default};
